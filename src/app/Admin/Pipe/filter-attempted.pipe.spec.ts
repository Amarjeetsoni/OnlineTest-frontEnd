import { FilterAttemptedPipe } from './filter-attempted.pipe';

describe('FilterAttemptedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAttemptedPipe();
    expect(pipe).toBeTruthy();
  });
});
